/**
 * Default values for all Application objects.
 *
 *   properties always start with default
 */
export const Defaults = {
  globalVersion: '0.0.1',
  globalLocale: 'en-US',
}

export const Globals = {
  globalLocale: 'fr-FR',
}
